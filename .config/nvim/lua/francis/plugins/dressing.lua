-- Dressing: Improves vim.ui.select() and vim.ui.input() with floating windows.
-- Makes rename prompts and code action menus look much nicer.
return {
    'stevearc/dressing.nvim',
    event = 'VeryLazy',
}
