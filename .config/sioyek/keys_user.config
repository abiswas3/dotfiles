# Toggle auto-highlight: when ON, any text you select with the mouse is
# highlighted automatically using the active highlight type (no h+letter needed).
toggle_select_highlight <C-h>

# Set the active highlight type/colour used by auto-highlight (prompts for a-z).
set_select_highlight_type <C-S-h>
